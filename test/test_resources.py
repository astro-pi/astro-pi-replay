import pytest

@pytest.mark.skip(reason="TODO")
def test_each_replay_dir_with_tests():
    # e.g. need to ensure that AstroX works with sense_hat
    # and AstroX
    pass

# also check that the order of precedence is being respected

[pytest]
pythonpath = src
; Uncomment the below to emit debug logs declaratively
; Otherwise, execute pytest with the following flags:
; 	-o log_cli=true --log-cli-level=DEBUG --log-format='[%(asctime)s] %(levelname)s %(name)s %(message)s'
; log_cli_level = DEBUG
; log_cli = true
; log_format = [%(asctime)s] %(levelname)s %(name)s %(message)s

